export interface UserInfoType {
    id: string;
    name: string;
    email: string;
    // qo'shimcha maydonlar bo'lishi mumkin
  }
  
  export interface ProductsPriceType {
    min: number | null;
    max: number | null;
  }
  